# Platform compatibility

Every platform works differently. When writing a mod, you may find that code that works perfectly on one platform completly breaks on another.
Geode provides the following platform definitions to allow developers to fix compatibility

```cpp
GEODE_IS_WINDOWS
GEODE_IS_ANDROID
GEODE_IS_MAC
GEODE_IS_MOBILE
GEODE_IS_X86
```
